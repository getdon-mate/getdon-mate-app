# v0.1.1 Operational Polish Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 협업 기능을 운영 도구 수준으로 다듬고, 모바일 밀도와 운영 회귀 테스트를 보강한다.

**Architecture:** 기존 reminder/board/calendar/statistics 상태를 유지하되, selector와 화면 구성만 확장해 최근 활동 노출과 날짜 집중 흐름을 만든다. 새 데이터를 크게 늘리지 않고 파생 데이터와 레이아웃 재구성으로 polish한다.

**Tech Stack:** Expo 54, React Native, React Navigation 7, TypeScript, Jest, Playwright

---

### Task 1: Lock failing coverage for operational activity flows

**Files:**
- Modify: `tests/unit/account-management-tabs.test.tsx`
- Modify: `tests/unit/selectors.test.ts`
- Modify: `tests/e2e/navigation-auth-regression.spec.ts`

**Step 1: Write the failing tests**

- Add coverage for recent reminder history, calendar date selection, and reminder-generated activity visibility.

**Step 2: Run tests to verify they fail**

Run: `pnpm test tests/unit/account-management-tabs.test.tsx tests/unit/selectors.test.ts --runInBand`
Expected: FAIL because the recent activity and focused calendar behavior do not exist yet.

**Step 3: Write minimal implementation**

- Add only the selectors and component hooks required to satisfy the new expectations.

**Step 4: Run tests to verify they pass**

Run: `pnpm test tests/unit/account-management-tabs.test.tsx tests/unit/selectors.test.ts --runInBand`
Expected: PASS

**Step 5: Commit**

```bash
git add tests
git commit -m "test: add ops polish coverage"
```

### Task 2: Surface reminder history and activity context

**Files:**
- Modify: `src/features/accounts/model/selectors.ts`
- Modify: `src/features/accounts/components/MemberRow.tsx`
- Modify: `src/features/accounts/components/detail-tabs/DuesTab.tsx`
- Modify: `src/features/accounts/components/detail-tabs/MembersTab.tsx`
- Modify: `src/features/auth/screens/NotificationListScreen.tsx`

**Step 1: Write the failing test**

- Add tests for showing the last reminder message and type near unpaid members.

**Step 2: Run test to verify it fails**

Run: `pnpm test tests/unit/account-management-tabs.test.tsx --runInBand`
Expected: FAIL because reminder history is not rendered yet.

**Step 3: Write minimal implementation**

- Add selectors for last reminder by member and use them in dues/member surfaces and notification list wording if needed.

**Step 4: Run test to verify it passes**

Run: `pnpm test tests/unit/account-management-tabs.test.tsx --runInBand`
Expected: PASS

**Step 5: Commit**

```bash
git add src/features/accounts src/features/auth/tests tests/unit/account-management-tabs.test.tsx
git commit -m "feat: surface reminder activity context"
```

### Task 3: Add calendar focus behavior and statistics summary polish

**Files:**
- Modify: `src/features/accounts/components/detail-tabs/CalendarTab.tsx`
- Modify: `src/features/accounts/components/detail-tabs/StatisticsTab.tsx`
- Modify: `src/features/accounts/model/selectors.ts`
- Test: `tests/unit/selectors.test.ts`

**Step 1: Write the failing tests**

- Add tests for focused day filtering and summary card values.

**Step 2: Run tests to verify they fail**

Run: `pnpm test tests/unit/selectors.test.ts --runInBand`
Expected: FAIL because focused calendar output and summary helpers do not exist yet.

**Step 3: Write minimal implementation**

- Add selector helpers and compact summary cards above the chart.
- Implement selected-day behavior in the calendar tab.

**Step 4: Run tests to verify it passes**

Run: `pnpm test tests/unit/selectors.test.ts --runInBand`
Expected: PASS

**Step 5: Commit**

```bash
git add src/features/accounts/components/detail-tabs src/features/accounts/model/selectors.ts tests/unit/selectors.test.ts
git commit -m "feat: polish calendar focus and statistics summary"
```

### Task 4: Tighten board flow and small-screen density

**Files:**
- Modify: `src/features/accounts/components/detail-tabs/BoardTab.tsx`
- Modify: `src/features/accounts/components/detail/DetailTabBar.tsx`
- Modify: `src/features/accounts/components/detail-tabs/TransactionsTab.tsx`
- Test: `tests/e2e/navigation-auth-regression.spec.ts`

**Step 1: Write the failing tests**

- Extend e2e for board posting, comment flow, and smaller control interactions without layout regressions.

**Step 2: Run tests to verify they fail**

Run: `pnpm test:e2e`
Expected: FAIL until the updated interaction model is in place.

**Step 3: Write minimal implementation**

- Reduce visual weight of the compose areas and tighten action paddings.
- Keep board/comment actions readable on mobile without growing vertical height too much.

**Step 4: Run tests to verify they pass**

Run: `pnpm test:e2e`
Expected: PASS

**Step 5: Commit**

```bash
git add src/features/accounts/components/detail src/features/accounts/components/detail-tabs tests/e2e
git commit -m "feat: tighten board flow and mobile density"
```

### Task 5: Final verification and merge

**Files:**
- Modify: `docs/plans/2026-03-15-v0-1-1-ops-polish-design.md`
- Modify: `docs/plans/2026-03-15-v0-1-1-ops-polish.md`

**Step 1: Run full verification**

Run: `pnpm test --runInBand && pnpm run ci:web && pnpm test:e2e`
Expected: PASS

**Step 2: Merge to main and re-run verification**

Run the same verification commands on merged `main`.

**Step 3: Push main**

Push the verified `main` branch to origin.
